import React from 'react';
import HeroBanner from '../../images/hero-banner-home.png';

export default ({}) => {
    return <div className="banner-image"><img src={HeroBanner} alt='the world we want'/></div>
}